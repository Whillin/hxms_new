VITE_PORT=3011
VITE_BASE_URL=/
# 显式关闭本地 mock，启用代理到后端
VITE_USE_MOCK=false
# 前端开发代理目标（本地后端 NestJS）
VITE_API_PROXY_URL=http://localhost:3002
# 是否携带跨域凭证（如需要可改为 true）
VITE_WITH_CREDENTIALS=false

# 可选：API_URL 仅用于日志展示，不影响请求
VITE_API_URL=http://localhost:3002/api
