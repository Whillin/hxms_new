VITE_VERSION=dev
VITE_PORT=3013
VITE_BASE_URL=/
# 仅用于日志输出，不参与请求。请求统一走相对路径 '/api'。
VITE_API_URL=http://localhost:3002/api
# 前端开发环境代理到本地后端端口（Nest 启在 3002）
VITE_API_PROXY_URL=http://localhost:3002
# 关闭本地 mock，走真实后端
VITE_USE_MOCK=false
# 携带 cookie 以便后端校验登录态
VITE_WITH_CREDENTIALS=true
