# Prevent secrets from entering build context
.env
.env.*

# Exclude local installs and build artifacts (reinstalled/built in image)
node_modules
.dist

# Optional VCS noise
.git
.gitignore

# Reduce build context further (dev-only)
exports/
test-ttl.ts